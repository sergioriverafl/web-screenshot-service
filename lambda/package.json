{
  "name": "screenshot-lambda",
  "version": "1.0.0",
  "description": "Lambda para captura de pantallas con Playwright",
  "main": "dist/handler.js",
  "scripts": {
    "build": "tsc",
    "dev:screenshot": "ts-node src/run-local.ts",
    "package:prod": "npm ci --omit=dev && npm run build && cd dist && zip -r ../function.zip . && cd .. && zip -r function.zip node_modules && npm install"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.600.0",
    "@sparticuz/chromium": "^123.0.1",
    "dotenv": "^17.4.2",
    "playwright-core": "^1.44.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.139",
    "@types/node": "^20.19.43",
    "typescript": "^5.4.5"
  }
}
